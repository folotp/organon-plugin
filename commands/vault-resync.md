---
description: Resolve vault-side drift detected by scripts/sync-vault.sh — bundled per-entry runbook
---

Load the `organon:vault-resync` skill via the Skill tool and follow it end-to-end. The skill's `SKILL.md` and `references/RESOLUTION_CHECKLIST.md` are the source of truth — do not improvise the resolution flow from this command body.
