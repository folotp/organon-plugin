---
description: Resolve kepano upstream drift detected by scripts/sync-kepano.sh — bundled per-section runbook
---

Load the `organon:kepano-resync` skill via the Skill tool and follow it end-to-end. The skill's `SKILL.md` and `references/RESOLUTION_CHECKLIST.md` are the source of truth — do not improvise the resolution flow from this command body.
