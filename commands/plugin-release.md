---
description: Cut a folotp/organon-plugin release — bump version, package, tag, GitHub Release with .plugin asset
---

Load the `organon:plugin-release` skill via the Skill tool and follow it end-to-end. The skill's `SKILL.md`, packaging script (`scripts/package.sh`), and `references/release-notes-template.md` are the source of truth — do not improvise the release flow from this command body.
