---
description: Three-pole drift audit (plugin reality + canonical vault note + per-surface implementations) over Organon-related memory and instruction stores. Surface-aware. Manual or scheduled.
---

Load the `organon:organon-memory-audit` skill via the Skill tool and follow it end-to-end. Forward any flags from the command invocation (`--scope=`, `--mode=`, `--surface=`) into the skill — they are parsed by the skill, not this wrapper. The skill's `SKILL.md`, `references/AUDIT_KEYWORDS.md`, and `references/REPORT_TEMPLATE.md` are the source of truth — do not improvise the audit flow from this command body.
